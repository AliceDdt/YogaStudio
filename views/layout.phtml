<!DOCTYPE html>
<html lang="fr">
<?php require 'views/inc/head.phtml'; ?>

<body>
    <div class="container">
<?php require 'views/inc/header.phtml' ?>
        <main class="main">
        <?php if (hasFlashes('error')): ?>
            <div class="alert error">
                <?php foreach (getFlashes('error') as $message): ?>
                <p><?=$message?></p>
                <?php endforeach?>            
            </div>
        <?php endif?>
        <?php if (hasFlashes('success')): ?>
            <div class="alert success">
                <?php foreach (getFlashes('success') as $message): ?>
                <p><?=$message?></p>
                <?php endforeach?>
            </div>
        <?php endif?>
            <?= $pageContent; ?>
        </main>
<?php require 'views/inc/footer.phtml' ?>
    </div>
    <script type="module" src="assets/js/main.js"></script>
</body>
</html>